services:
  - type: web
    name: truelink-api
    env: python
    buildCommand: python3 -m ensurepip --upgrade && pip install --upgrade pip && pip install --upgrade truelink && pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port 10000
    plan: free
